### Model Configuration
# Specifies the base model for full parameter fine-tuning
model_name_or_path: ZhipuAI/glm-4-9b-chat  # GLM-4 9B chat model from ZhipuAI

### Training Method
# Defines full parameter fine-tuning with DeepSpeed optimization
stage: sft  # Supervised fine-tuning stage
do_train: true  # Enable training mode
finetuning_type: full  # Full parameter fine-tuning (all weights updated)
deepspeed: examples/deepspeed/ds_z3_config.json  # DeepSpeed ZeRO-3 config for memory efficiency

### Dataset Configuration
# Specifies the training dataset and preprocessing settings
dataset: huatuo  # Huatuo medical encyclopedia QA dataset
template: glm4  # Chat template for GLM-4 model
cutoff_len: 1024  # Maximum sequence length (tokens)
max_samples: 100000  # Maximum training samples (use full dataset)
overwrite_cache: true  # Regenerate preprocessed cache
preprocessing_num_workers: 16  # Parallel workers for data preprocessing

### Output Configuration
# Defines where to save checkpoints and logs
output_dir: /root/code/checkpoints/qwen  # Checkpoint save directory
logging_steps: 10  # Log metrics every N steps
save_steps: 500  # Save checkpoint every N steps
plot_loss: true  # Generate loss curve plot
overwrite_output_dir: true  # Overwrite existing output directory

### Training Hyperparameters
# Core training settings for full fine-tuning
per_device_train_batch_size: 1  # Batch size per GPU
gradient_accumulation_steps: 2  # Accumulate gradients over N steps (effective batch size = 1 * 2 = 2)
learning_rate: 1.0e-5  # Initial learning rate (lower than LoRA due to full parameter updates)
num_train_epochs: 1.0  # Number of complete passes through the dataset
lr_scheduler_type: cosine  # Learning rate schedule (cosine annealing)
warmup_ratio: 0.1  # Warmup for first 10% of training steps
bf16: true  # Use bfloat16 mixed precision (requires Ampere+ GPUs)
ddp_timeout: 180000000  # Distributed training timeout (milliseconds)

### Evaluation Configuration
# Settings for validation during training
val_size: 0.05  # Use 5% of training data for validation
per_device_eval_batch_size: 1  # Batch size for evaluation
eval_strategy: steps  # Evaluate every N steps
eval_steps: 500  # Run evaluation every 500 training steps